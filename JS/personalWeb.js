var message=getElementById("message");
var comment=getElementById("comment");

message.addEventListener("click", handle);
comment.addEventListener("click", handle);

function handle() {
  $.ajax({
    
  })

}